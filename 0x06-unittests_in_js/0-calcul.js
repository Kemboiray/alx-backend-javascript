// This file defines `calculateNumber`, a function that rounds two numbers and
// returns the sum of those rounded numbers.

function calculateNumber(a, b) {
  return Math.round(a) + Math.round(b);
}
